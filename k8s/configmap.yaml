apiVersion: v1
kind: ConfigMap
metadata:
  name: llm-dbms-config
data:
  ENVIRONMENT: "production"
  DATABASE_TYPE: "postgresql"
  LLM_PROVIDER: "openai"
  SAFETY_MODE: "strict"
  API_HOST: "0.0.0.0"
  API_PORT: "8000"
